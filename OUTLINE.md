## css 相关

- CSS line-height
- CSS 伪类和伪元素

- CSS 单行和多行截断
- CSS 画出一个三角形

- html meta 标签有啥作用
- CSS <link/>为什么要放在头部
- BFC 块级格式化上下文
- less,sass 它们的作用是什么
- CSS3 新特性
- CSS 硬件加速

- CSS 居中的方法
- CSS 栅格布局
- 上中下三栏布局
- 双栏固定布局
- 宽是高的一半的垂直居中，里面有字体也要垂直居中类数组

## js 语言

- 正则匹配邮箱
- 正则一些概念
- JavaScript this 的指向；箭头函数的 this 指向
- promise, setTimeout, requestAnimationFrame 的区别
- Promise / setTimeout 的执行顺序；实际考察知识点：对「事件队列 / 宏任务 / 微任务」的了解
- promise async setTimeout 先后次序
- 用 promise 来处理一个 const
- promise.all
- promise 封装 setstate
- 怎么将一个异步方法 promise 化
- 前端做并发请求控制
- 并发请求最大值是 10，怎么处理队列
- 函数并发任务控制
- for of
- setTimeout 动画与 CSS animation 区别

- 节流 & 防抖

- 实现 EventBus
- .js 作为弱类型语言，为什么要有 float32Array 这种强类型
- interface 他只想继承一些属性
- [1,2,3].map(parseInt)
- div 的点击事件回调不执行的原因，具体的一种原因怎么定位问题

- 自己实现 bind 函数
- bind 函数运行结果
- 多个 bind 连接后输出的值

- instanceof

* 事件委托
* ==的隐式转化
* 浅拷贝与深拷贝区别

* js 实现依赖注入

* 类式继承的方案
* 三个继承方式的优缺点
* js 原型继承 & 原型链
* 实现一个子类实例可以继承父类的所有方法

* fetch 取消

## DOM

- click DOM 节点的 inner 与 outer 的执行机制，考查事件冒泡与事件捕获 （看试题给结果）
- 结合事件代理

## 图形

- canvas 和 svg 的区别

## 前端性能优化

- 协商缓存
- 前端缓存、回话机制
- 打开谷歌浏览器会有多少进程
- 浏览器缓存的方法有哪些，它们的优先级是怎样的
- 浏览器 reflow 和 repaint
- webpack 性能优化

## 前端安全

- 前端攻击方式和防御方式
- csrf/xss 攻击原理
- 前端安全 ， DOS
- 跨域
- jsonp 的实现（要代码）方式

## 网络

- http 的方法有哪几种
- http 请求都包含哪些字段
- http 请求幂等性
- cookie 结构有什么字段
- 状态码 304 是什么意思
- 都说要减少 https 的请求，https 为什么慢
- https 握手过程
- https 协议的过程
- https 获取加密密钥的过程
- http2 与 http1 有什么区别
- 计算机网络知识 TCP UDP
- tcp 为什么是可靠的

## 解决方案

- 长列表渲染
- 前端渲染大量数据的优化
- 获取页面所有 img 并且下载
- 两个同源 tab 之间的交互，数据同步
- 实现 当 ul 点击时 输出 子元素 li 的内容
- 富文本编辑器的实现
- 文件上传的实现；
- autocomplete 组件
- 实现一个 outsideclick 的 Hoc，触发时调用 子组件的 outsideclick 方法
- ssr（ssr csr 混合怎么处理）
- 说说单点登录的实现
- 牛客网的在线代码编辑器需求，如果交由你来设计，说说你的思路
- 如果上线了一个页面，有用户反馈他的手机打开白屏，你会怎么定位并解决这个问题
- js 多线程如何共享大的数据

## react

- react 生命周期
- react diff 算法
- react key 的作用，setState 的机制，
- react 合成事件原理
- react 新版本的特性
- React Fiber 原理以及为什么 componentWillRecievedProps 会废弃
- React Hook 原理
- getDerivedStateFromPros 为什么是 Static
- redux
- redux 异步中间件原理

## 算法

- 快速排序的实现
- 冒泡算法
- 最长子序列

- 二叉树遍历
- 二叉树中序遍历
- 二叉树搜寻算法
- 二叉树是否存在某个路径的和等于一个值
- 二叉树路径总和(leetcode 112)
- 二叉树深度为 n 的遍历，遍历有哪几种方式
- 树的遍历有几种方式，实现下层次遍历
- 判断对称二叉树

- 股票买卖问题，输入一天内的股票价格数组，要求写出算法

- 从排好序的两个链表中，找到相同的节点，并输出链表（看试题、现场写程序）
- 根据条件获取递归树中过的某一节点

- 输入一个日期 返回几秒前 几天前或者几月前；
- 请实现如下的函数，可以批量请求数据，所有的 URL 地址在 urls 参数中，同时可以通过 max 参数控制请求的并发度，当所有请求结束之后，需要执行 callback 回调函数。发请求的函数可以直接使用 fetch 即可
- versions 是一个项目的版本号列表，因多人维护，不规则
  var versions=['1.45.0','1.5','6','3.3.3.3.3.3.3']
  要求从小到大排序，注意'1.45'比'1.5'大
  sorted=['1.5','1.45.0','3.3.3.3.3.3','6']
- 算法题，把数组中的 0 移动到数组头部
- 数组去重
- 给定一个数组，一个期望值，找到数组中两个相加等于期望值
- 给定一个不含重复数字的数组 arr,指定个数 n,目标和 sum,判断是否含有由 n 个不同数字相加得到 sum 的情况（leetcode 40 变种， 数字不得重复使用）
- 数字千分位处理（正则）
- 设计一套机制，两个千位大数相乘
- 多空格字符串格式化为数组
- 实现 sum(1)(2)(3).valueOf()，实现这么一个 sum 函数，返回 6
- taskSum(1000,()=>{console.log(1)}).task(1200,()=>{console.log(2)}).task(1300,()=>{console.log(3)})，这里等待 1s，打印 1，之后等待 1.2s，打印 2，之后打印 1.3s，打印 3
- 给一个由域名组成的字符串进行按子域名分组的反转，比如 news.toutiao.com 反转成 com.toutiao.news 需要 in place

- 事件循环

## websocket

- websocket 中有用到二进制类型的数据吗，还是只用过 json 字符类型的数据

## CI/CD

- 测试 单测、集成测试
- 自动化集成
- Docker

## 经历

- 讲一件自己记忆深刻的事情
- 接下来前端要深入的方向
- 身边优秀的同事，比你优秀的原因是什么，以及你比其他人突出的点
- 你选择一家新公司，会基于哪些点来考虑
- 最近在做什么项目，为什么做，解决了什么问题，遇到了什么问题
- 在现团队中担任的角色；觉得自己印象最深刻,
- 最有亮点的项目经历,不是技术细节了，更多的是大方向思考技术以及技术深入研究
- 自己做得最有成就的项目
- 自己主动承担并是核心的项目
- 职业发展

## 暂时不选择的方向

- mvvm
